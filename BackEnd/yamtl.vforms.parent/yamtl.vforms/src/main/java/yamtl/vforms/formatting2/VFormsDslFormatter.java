/*
 * generated by Xtext 2.30.0
 */
package yamtl.vforms.formatting2;

import org.eclipse.xtext.formatting2.AbstractJavaFormatter;
import org.eclipse.xtext.formatting2.IFormattableDocument;
import yamtl.vforms.vFormsDsl.FormInput;
import yamtl.vforms.vFormsDsl.FormInputSelect;
import yamtl.vforms.vFormsDsl.Model;

public class VFormsDslFormatter extends AbstractJavaFormatter {

	protected void format(Model model, IFormattableDocument doc) {
		// TODO: format HiddenRegions around keywords, attributes, cross references, etc. 
		doc.format(model.getFormLayout());
		for (FormInput formInput : model.getFormInput()) {
			doc.format(formInput);
		}
	}

	protected void format(FormInputSelect formInputSelect, IFormattableDocument doc) {
		// TODO: format HiddenRegions around keywords, attributes, cross references, etc. 
		doc.format(formInputSelect.getOption());
	}
	
	// TODO: implement for DataOption, EnumOption, FormInputSearch, DataOptionItem, FormInputGroup
}
